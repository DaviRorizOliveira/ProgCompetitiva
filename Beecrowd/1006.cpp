#include <bits/stdc++.h>

using namespace std;

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    double A, B, C;
    cin >> A >> B >> C;
    printf("MEDIA = %.1f\n", ( (2 * A) + (3 * B) + (5 * C) ) / 10);
    return 0;
}